import 'package:flutter/material.dart';

class EditarPetPage extends StatelessWidget {
  const EditarPetPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
