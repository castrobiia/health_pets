import 'package:flutter_test/flutter_test.dart';
import 'package:health_pets/http/vacina-repository.dart';

main() {
  //final repository = VacinaRepository();
  test('deve trazer uma lista de VacinaModel (data de aplicação)', () async {
    //final list = await repository.fetchVacina();
    //print(list[0].dataAplicacao);
  });
}
